import { View, Text } from "react-native";
import React from "react";
import { SafeAreaView } from "react-native-safe-area-context";

const QuizAnimals = () => {
  return (
    <SafeAreaView>
      <Text>QuizAnimals</Text>
    </SafeAreaView>
  );
};

export default QuizAnimals;
